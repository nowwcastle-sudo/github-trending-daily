# GitHub Trending Daily multilingual production closure handoff

- 최초 작성: 2026-08-30 KST
- 현재 개정: 2026-09-01 KST
- 저장소: `https://github.com/nowwcastle-sudo/github-trending-daily`
- 운영 페이지: `https://nowwcastle-sudo.github.io/github-trending-daily/`
- 작업 worktree: `C:\Users\nasca\.codex\worktrees\3a3b\transactional-refresh-20260827`
- production RED source SHA: `0aa617016c0e832909f74e3d9a70bbe210c10d60`
- 2026-08-31 첫 controlled dispatch 좌표: clean `HEAD == origin/main == c33b7e449aa3b225130e771b530215c7ee2a75cf`, workflow run `33337114759` (remote write 직전 다시 fetch)
- 2026-08-31 두 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 6e6f4749178094782557987d86da2e3385a60b08`, workflow run `33338119441`
- 2026-08-31 세 번째 controlled dispatch 좌표: clean `HEAD == origin/main == d3c8fdc3f61ca816104985d7bda40c9e725954be`, workflow run `33340906781`
- 2026-08-31 네 번째~여섯 번째 controlled dispatch 좌표: clean `HEAD == origin/main == dfa3bd387c40c104b15abf861bb9f4c003fb7902`, workflow runs `33353784160`, `33355529363`, `33356905445`
- 2026-08-31 일곱 번째 controlled dispatch 좌표: clean `HEAD == origin/main == c83aa52c4f9fb7c79742a217a0ccae91a4726be6`, workflow run `33358622337`
- 2026-08-31 여덟 번째 controlled dispatch 좌표: clean `HEAD == origin/main == a1d04d5505929d298a2030e75eec01fb288ded3f`, workflow run `33360314788`
- 2026-08-31 아홉 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 49596f97fad19cb5b4b732388165b9f9b37b3824`, workflow run `33361877370`
- 2026-08-31 열세 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 3e4ac55d3d2a0e7a153750a75133dcc692312482`, workflow run `33367283210`
- 2026-08-31 열네 번째 controlled dispatch 좌표: clean `HEAD == origin/main == bf5c28f97a577d3ade412e4ef7d06bd078e3a312`, workflow run `33370143480`
- 2026-08-31 열다섯 번째 controlled dispatch 좌표: clean `HEAD == origin/main == d8c6cdb717636ba45cff0576800d6b276fb300c0`, workflow run `33372034170`
- 2026-08-31 열여섯 번째·열일곱 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 80f3e6cccee30a8c2a4372eb0deb33697ea86600`, workflow runs `33374144042`, `33375183865`
- 2026-08-31 열여덟 번째 controlled dispatch 좌표: clean `HEAD == origin/main == aca6b975a8c97f992edae6b26ae22e85931516f6`, workflow run `33378871320`
- 2026-08-31 열아홉 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 7ee589c2d2952f51db50d1e9e615dac0455ea2b3`, workflow run `33383238462`
- 2026-08-31 스무 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 208f633eced8b7d1753ef73bed255b384b3a3e74`, workflow run `33388405416`
- 2026-08-31 스물한 번째·스물두 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 0205981bd9f9882b6dd67ac323cc00bac9122d6f`, workflow runs `33392159705`, `33392487673`
- 2026-08-31 스물세 번째 controlled dispatch 좌표: clean `HEAD == origin/main == a37f56d3eee0c23c72c88325f3aa7986163e11e9`, workflow run `33395938647`
- 2026-09-01 서른두 번째 controlled dispatch 좌표: clean `HEAD == origin/main == 15837bf3d780e15d643fdd6d857e536bd4cebd4e`, workflow run `33440722982`
- AgentMemory handoff: `mem_mtfb5jzh_7ddf0a27f5dc`
- 원본 task: `01a03d80-1266-76e1-896d-16648967d258`

## 0. 이 문서의 지위

이 문서는 2026-08-30 production closure의 최신 정본이다. 오래된 progress·ledger·이전 README 번역 계획보다 현재 사용자 결정, 코드, 테스트, Actions, Pages, production 실측을 우선한다. 원격 자동 갱신이 있을 수 있으므로 push와 배포 직전에 `git fetch --prune origin`으로 좌표를 다시 확정한다.

2026-09-01 후속 계약이 이전 UI 설명을 대체한다. branch `codex/fix-period-locale-sidebar-20260901`, base `084a657d1bb36c7a79b3005161c50ddabf4938c0`에서 site locale 단독 summary, hover 0ms close 시작+210ms transition, 모바일 visible button 제거, 실제 period membership/gain/HOT/All, neutral migration baseline과 S1/S2/S3 전이를 구현·검증해 PR #34를 만들었다. current production 성공·merge 준비·배포 준비로는 아직 부르지 않는다. `wait-what` 발동은 0회다.

### 2026-09-01 release·redeploy 후속 계약

- **Code release**는 현재 Pages code bytes로 새 v1 snapshot을 record, derive, finalize한 뒤 배포한다.
- **Finalized artifact redeploy**는 이미 finalize된 source와 byte-for-byte 같은 artifact만 다시 배포한다. old finalized contract 아래에서 Pages bytes가 바뀌면 builder는 artifact·manifest 출력 전에 중단하고 full refresh를 요구한다. `.github/workflows/deploy-current-pages.yml`은 refresh, DB write, Claude·Codex 호출 없이 이 재배포만 수행한다.
- 예약 Daily Refresh의 기본 summary producer는 계속 Claude CLI OAuth `claude-sonnet-5`다. Codex fallback은 같은 frozen input에서 exact pending repository에만 적용하며 이미 완료된 repository를 재생성하거나 예약 기본 producer를 대체하지 않는다.
- 이 후속 계약은 아래 과거 controlled dispatch·production 성공·실패 기록을 수정하거나 소급 재해석하지 않는다.

## 1. 현재 사용자 결정

1. `README.md`는 영문 정본으로, `README.ko.md`는 전체 한글판으로 유지한다. `README.en.md`는 기존 링크 호환 안내만 둔다.
2. 사이트의 안정적인 기본 틀은 영어·한국어·중국어 간체·스페인어·일본어 5개 언어로 전환한다. 갱신 때 바뀌는 repository card 원천 데이터는 자동 번역하지 않는다.
3. repository README 전체 생성 번역은 폐기한다. README viewer에는 upstream 저장소가 실제 제공하는 언어판만 표시한다.
4. summary tooltip은 사이트 locale 하나만 따르며 내부 언어 선택 row를 두지 않는다. exact locale bundle이 불완전하면 다른 locale로 fallback하지 않고 localized unavailable을 표시한다.
5. **View README**와 `goal/usage/pros/cons/fit`은 site locale에 결합된 단일 tooltip content path에 둔다.
6. sidebar는 사용자가 선택한 B안인 **Compact Rail**로 확정한다. 데스크톱 hover는 비모달이며 결합 영역을 벗어나면 0ms에 210ms close transition을 시작한다. click·keyboard 실행은 focus trap 모달이고, 모바일은 right-edge swipe open·left swipe close가 기본이며 native button은 screen reader·hardware keyboard용 visually-hidden 진입점으로만 유지한다.
7. summary producer는 Windows self-hosted runner의 `claude -p --model claude-sonnet-5`와 first-party OAuth subscription을 사용한다. direct Anthropic Messages API, API key, dollar-cost planning은 폐기한다.
8. runner는 repository 전용 label `gh-trending-claude`를 요구하며, 사용자 범위 `CLAUDE_CODE_OAUTH_TOKEN`만 Claude step 안에서 가져온다. Claude 자식 환경은 positive allowlist이고 도구·slash command·Chrome·session persistence를 끈다.
9. controlled workflow와 Pages 배포는 로컬 검증·비밀값 검사·remote drift·runner online/OAuth preflight가 모두 통과한 뒤 6단계 말미에 수행한다. 사용자는 publication blocker를 역추적해 고친 뒤 전체 workflow와 Pages가 안정적으로 완주할 때까지 필요한 controlled dispatch를 승인했다. 실패 로그를 읽지 않은 blind retry는 금지한다.
10. repository observation DB에는 README 전체 본문을 저장하지 않는다. source identity, hash, schema, state만 저장한다.
11. Codex Security Deep Scan은 사용자 결정으로 폐기됐다. 재개·대체·완료 게이트·잔여 위험으로 취급하지 않는다.
12. CodeQL, 정적·의존성·비밀값 검사, Firestore Rules, 실제 로그인과 나머지 production 인수시험은 생략하지 않는다.

## 2. production RED 기준선

- 기준선 당시 clean `HEAD == origin/main == ls-remote main == 0aa617016c0e832909f74e3d9a70bbe210c10d60`.
- production manifest version 0, `legacyBootstrap: true`, `sourceSha: 0aa6170…`, `snapshotId: null`.
- active repository 49개 중 49개가 정확히 `구체적인 설치 및 사용 절차는 저장소 README 원문을 확인한다.`라는 metadata-only 일반 summary를 사용한다.
- 49개 모두 유효한 `readme_path`, `readme_blob_sha`, `readme_content_sha256`, `default_branch_head_sha` provenance가 없다.
- legacy translation file은 33개이고 16개가 없지만, 새 설계에서는 이 full README translation을 성공 조건으로 사용하지 않는다.
- active source registry는 version 2 legacy이며 `model/blob_sha/translation_applicable`가 null이다. v3 summary provenance 성공 증거가 아니다.
- migration gate는 위 상태를 정확한 `legacy_red`로만 인정하며 production success로 승격하지 않는다.

## 3. summary 품질 정본

이 사양은 `mattpocock-skills:grilling`로 사용자와 공동 확정했다.

1. 사용자가 30~60초 안에 기술적 적합성을 판단할 수 있는 요약이다.
2. 필드는 정확히 `goal`, `usage`, `pros`, `cons`, `fit`이며 서로 중복하지 않는다.
3. 영어 환산 전체 180~280단어, 목표 200~240단어다.
4. README 사실과 직접적이고 신중한 함의만 허용한다.
5. README에 실제 있는 핵심 명령만 1~2개 포함한다.
6. 중립적 기술 문체를 사용하고 홍보성 최상급 표현은 거부한다.
7. 5개 locale의 command, URL, version, number, product name, warning은 정확히 같아야 한다.
8. 5개 언어는 한 묶음이며 한 언어의 결함도 repository와 candidate 전체를 실패시킨다.
9. `insufficient_source`는 candidate 실패다. metadata fallback이나 “README 참고” 문구로 통과시키지 않는다.
10. normal refresh의 전체 12 retry는 유지한다. 승인된 최초 v0 bootstrap은 repository마다 최대 3회의 품질 교정을 수행할 수 있도록 pending 수에 비례한 유한 cap을 사용한다. deterministic validator가 허용된 locale/field 또는 invariant 선언을 특정하면 해당 위치만 보정 prompt에 전달한다.
11. 모델은 내부 evidence의 README line range와 invariant의 `kind`/`value`만 반환한다. section heading은 frozen README의 ATX/Setext 구조에서, invariant field 위치는 실제 5-locale summary에서 결정적으로 파생해 저장하며 기존 cache의 파생값이 다르면 거부한다. README 전체 본문은 observation DB에 넣지 않는다.
12. UI에는 “verified repository README를 바탕으로 AI가 생성”했다고 표시하고 human verified라고 하지 않는다.
13. reuse는 동일 README blob/content hash, prompt schema, `claude -p` provider/interface, CLI version, OAuth auth method일 때만 허용한다.
14. 첫 production은 automated full census와 층화 10-repository × 5-locale 수동 표본을 web/mobile 양쪽에서 수행한다.

## 4. 구현된 계약

- `site-i18n.js`: exact locales `en`, `ko`, `zh-CN`, `es`, `ja`; 저장값 → browser locale → English 순으로 선택한다.
- `index.html`: 64px Compact Rail, site locale 단독 summary, 별도 summary tabs 제거, hover 즉시 close, 모바일 hidden keyboard 진입점과 edge swipe, period별 gain/HOT 및 All total-stars-only render.
- `scripts/readme-variants.mjs`: 같은 디렉터리의 deterministic upstream README aliases만 수집하고 exact path/blob/head/content hash를 검증한다.
- `scripts/claude-cli-runtime.mjs`: Claude Code 2.1.211 이상, first-party OAuth, positive-allowlist child environment, tool-free structured output, 8 MiB stdin, timeout·output cap·Windows process-tree 강제 종료를 구현한다.
- `scripts/generate-summary-bundles.mjs`: `claude -p` Sonnet 5, schema v3, prompt schema v3, atomic 5-locale output, 최대 세 번의 targeted quality correction, 전체 retry cap과 CLI/OAuth provenance를 구현한다. normal refresh의 retry cap은 12로 유지하고 승인된 최초 v0 bootstrap만 pending repository당 세 번의 유한 correction capacity를 갖는다. 모델은 `start_line`/`end_line`과 invariant `kind`/`value`만 반환하고, section heading과 invariant field 위치는 frozen README와 실제 5-locale summary에서 결정적으로 파생한다. inference field는 5개 locale 모두 자연어 hedge를 명시해야 하며 누락 시 exact locale/field와 그 언어의 허용 강도 예시를 correction에 전달한다. 한 repository가 영구 실패하면 다른 worker는 새 repository 요청을 시작하지 않는다.
- `scripts/collect-repository-events.mjs`: 최대 75개 repository와 각각 최대 2 MiB canonical README 및 envelope 여유분을 수용하는 320 MiB frozen-facts 상한과 strict parser를 단일 정의하며, enrichment·render·coverage·Codex adapter 등 모든 consumer가 같은 parser를 사용한다. upstream variant는 identity metadata만 frozen facts에 포함되고 본문은 포함하지 않는다. GitHub 요청은 기존 3회(2초·8초), 반복 500이 관측된 OSS Insight 요청만 5회(2초·8초·30초·60초)로 bounded retry하며 끝까지 실패하면 candidate 전체를 fail closed한다.
- `scripts/update-trending.mjs`: canonical README 개별 상한은 2 MiB이고, immutable blob/path/head/content hash와 strict UTF-8/control-character gate를 요구한다.
- `scripts/update-trending.mjs`: v3 source와 exact 5-locale summary bundle만 render하며 metadata fallback을 허용하지 않는다.
- `scripts/validate-enrichment-coverage.mjs`: active page/facts/cache/source exact set, full envelope, locale completeness, stale/missing/insufficient source, translation residue를 fail closed로 검사한다.
- `scripts/build-pages-artifact.mjs`: source registry v3와 `site-i18n.js`를 exact artifact에 포함하고 legacy translation artifact를 제외한다.
- workflow는 GitHub-hosted `prepare` → Windows self-hosted `enrich` → GitHub-hosted `publish`로 분리한다. self-hosted runner는 tracked checkout·DB·page·commit·Pages 권한이 없고 네 개의 allowlisted enrichment 파일만 내보낸다. manual bootstrap gate는 승인 상태이고 schedule은 계속 hold한다. 열일곱 controlled run 모두 publish 전 fail-closed로 종료됐다.
- runner `nasca-gh-trending-claude`는 `C:\actions-runner-gh-trending`의 공식 Actions Runner 2.337.0으로 등록했다. Task Scheduler 경로에서는 listener가 job을 받은 뒤 Worker IPC가 44초간 멈춰 `steps=0`으로 실패했지만, interactive listener에서는 Worker·checkout·Claude step이 정상 실행됐다. 현재 production closure 동안은 interactive listener를 사용하고 schedule은 hold한다.
- `README.md`, `README.ko.md`, `README.en.md`와 2026-08-31 candidate screenshots를 새 구조에 맞췄다.

## 5. canonical 실행 계획

1. **Baseline·RED — 완료**: npm/Python 기준선, 49/49 generic, 0/49 provenance, 33/49 legacy translation, same-run ordering 실패를 자동 계약으로 고정한다.
2. **B Compact Rail·5-locale shell·영문 기본 문서 — 완료**: UI와 정적 문서를 새 구조로 전환한다.
3. **Upstream README variants — 구현 완료, production data 대기**: 실제 존재하는 언어판만 exact source identity로 표시하고 full README translation path를 폐기한다.
4. **Grilling 기반 summary 사양 — 완료**: 위 품질·길이·근거·동일성·실패 계약을 공동 확정한다.
5. **Sonnet 5 producer·coverage gates — 코드·로컬 검증 완료**: local `claude -p`, first-party OAuth, 고정 byte/retry/timeout cap, producer provenance, atomic 5-locale bundle 계약을 사용한다.
6. **검증·commit/push·controlled workflow·Pages — 여덟 번째 dispatch의 inference-order correction repair 검증 완료**: 기존 390/720/1200/1440, 5 locale, hover/click/focus/Escape, light/dark, tooltip, README legacy failure UI, export privacy, Atom/membership 실브라우저 검증을 보존한다. 첫 일곱 run의 원인과 frozen evidence는 위 이력대로 유지한다. 여덟 번째 run `33360314788`은 prepare를 5분 39초에 완주하고 interactive runner에서 실제 Claude enrichment를 8분 48초 수행한 뒤 `Summary bundle inference field set is invalid`로 fail-closed 종료됐다. structured-output schema는 `inference_fields`의 enum·unique만 보장하지만 validator는 `goal, usage, pros, cons, fit` canonical order를 요구했고, 기존 initial/correction prompt는 그 순서를 설명하지 않았다. exact completed model calls와 tokens는 usage receipt가 없어 `unknown`이며 DB·commit·Pages·probe는 0이다. 해당 frozen input은 snapshot `20260831052234-795cef5ad0be9d73`, repositories `50`, `sourceSetSha256=a8f5411c72c0128ed980955f1508f0a4c97c26921ff6d72a1949ab21d91364da`, `runContextSha256=7ff72823815f7bcea372a62e4c913d2cad31c927460df08872192588ffa588f5`, `factsSha256=69bea7ba4c78fc70f55914704a24fadcb73162483b48387e59835230801b70e3`, `eventsSha256=dfb2fda15402588f34669356947bfd7c2e7cf65f8d56d3f78ba4f8de0b9f7642`다. 새 RED는 두 번의 out-of-order inference set 뒤 canonical set을 요구하며 기존 prompt에서 실패했고, initial prompt와 exact correction에 canonical order를 명시해 green이 됐다. 전용 correction을 무력화한 변이는 RED를 다시 실패시켰다. 최종 local 기준선은 Node 519개(507 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass다.

**6단계 최신 후속 — 아홉 번째 dispatch의 concrete-cons repair 검증 완료**: run `33361877370`은 prepare를 5분 26초에 완주하고 실제 Claude enrichment를 10분 32초 수행한 뒤, 두 번의 기존 locale/field correction에도 `Summary bundle contains a generic or placeholder es.cons`로 fail-closed 종료됐다. 기존 correction은 위치와 “README를 보라고 하지 말라”는 금지만 전달해 source가 명시적 단점을 주지 않을 때 대체할 `cons` 유형을 제시하지 못했다. retry/cap/validator를 바꾸지 않고 initial/correction prompt에 source-supported prerequisite, limitation, operational trade-off, cautiously worded documentation gap 중 하나를 쓰도록 명시했다. tight RED는 같은 invalid `es.cons`가 두 번 이어진 뒤 valid response를 요구하며 새 구체 지침을 검사하고, cons 전용 correction을 무력화한 변이는 다시 실패했다. frozen input은 snapshot `20260831054926-fed9cec3abd9f8d9`, repositories `50`, `sourceSetSha256=b0beee6143ea9a0a925eb7ce65fa7009dad3ed7b9dcedb6d4843188820b948be`, `runContextSha256=93cb15e562b7edc516c5e0225f139cae96045c9d9256ad2e53d3da717cd44f8e`, `factsSha256=55717ab6913daea38711f8f3e0deeab9c059335098afe06c7cb76d8a07c1ca6b`, `eventsSha256=85db27b6777121be4f0e18b3e415c224807e5df38acbfd6a0d51eccdca300ef8`다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 DB·commit·Pages·probe는 0이다. local 기준선은 Node 519개(507 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass다.

**6단계 최신 후속 — 열 번째·열한 번째 dispatch의 OSS Insight 반복 500 안정화 검증 완료**: main `f13c97c8ab9c51eea1f9b0f59d3f3c47697722c6`은 CodeQL run `33363330706`에서 JavaScript/TypeScript 87규칙·Python 43규칙의 실제 analysis 2건, results 0, open alerts 0을 통과했다. run `33363575020`은 snapshot `20260831061716-b2876a057ffd00d2`, `sourceSetSha256=29002662de9803f53c18af5f28a8be0408e71ffc66dfbc4817a32ad7cb3fbe8d`, `runContextSha256=8b8cddb50664252c5fb6d00a7a6e99b2875db05da23f438d00285f4d25af2d20`에서 `tashfeenahmed/freellmapi` OSS Insight 500으로 종료됐다. endpoint가 200으로 회복한 것을 상태·길이·SHA-256만으로 확인한 뒤 실행한 run `33363947539`도 snapshot `20260831062244-7cb96fb7e0200576`, `sourceSetSha256=abbefe9543b2a4783d61f548f97c566cf2f5f383020abd546241ec361c82eb43`, `runContextSha256=daefaafa3108042370d84391e00d8c00d92417e6e4ba33ae3f45121b55675a02`에서 다른 `earendil-works/pi` OSS Insight 500으로 종료됐다. 둘 다 prepare 실패라 Claude·DB·commit·Pages·probe는 0이고 refresh-input artifact도 생성 전이라 facts/events digest는 없다. 데이터 누락을 허용하지 않고 OSS 요청에만 5회 bounded recovery schedule을 적용했으며, 4회 500 뒤 5회째 성공과 5회 전부 500인 terminal failure를 RED로 고정했다. 기존 3회 policy 변이를 주입하면 새 테스트가 실패함을 확인했다. 최종 local 기준선은 Node 520개(508 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 열두 번째 dispatch의 locale inference-strength repair 검증 완료**: OSS retry repair는 PR #12와 main `64a840f8055fa04e2bbf5b8e0d67528d7872b006`의 CodeQL 실제 analysis 2건(87·43규칙), results 0, open alerts 0을 통과했다. run `33365422799`은 prepare를 5분 24초에 성공해 반복 OSS 500 blocker가 production 입력에서 제거됐음을 증명했고 Claude enrichment를 8분 15초 수행한 뒤 `Summary bundle inference strength is missing in ja.cons`로 fail-closed 종료됐다. validator는 inference field의 모든 locale에 명시적 hedge를 요구하지만 기존 correction은 이 오류를 `OUTPUT_SCHEMA`로만 전달해 exact locale/field와 수정 강도를 잃었다. initial prompt에 every-locale explicit hedging 계약을 명시하고, correction에 exact locale/field와 해당 언어의 허용 hedge 예시를 전달했다. 같은 invalid `ja.cons`가 두 번 이어진 뒤 valid 일본어 hedge를 요구하는 RED를 추가했고 inference 전용 branch를 무력화한 변이에서 실패함을 확인했다. frozen evidence는 `C:\Users\nasca\AppData\Local\Temp\gh-trending-run-33365422799-67e19ba5f0ce49a2b762475c8e9ac33a`, snapshot `20260831064441-0aafed2299191a62`, repositories `50`, `sourceSetSha256=c2ed48eb92384f62d22419ed4aa36ca37e0798fa51a83a65cf94190d4ccc0f85`, `runContextSha256=6aa69942359a266a8c238c06d7f7e6a945c6f5dd5557c51d2a45ba552c0fce2a`, `factsSha256=f092f2776bba5e816a44e98c242583e19f6b94885ca2f299d4aadd93154601f7`, `eventsSha256=f7bfc47d3521c632c68b6e899f3d06bf4899ba4133d82bf43a8d4ac17a8b9350`이다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 DB·commit·Pages·probe는 0이다. 최종 local 기준선은 Node 521개(509 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

**6단계 최신 후속 — 열세 번째 dispatch의 invariant-field correction 검증 완료**: inference-strength repair는 PR #13과 main `3e4ac55d3d2a0e7a153750a75133dcc692312482`에서 CodeQL 실제 analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. run `33367283210`은 prepare를 5분 29초에 성공하고 Claude enrichment를 11분 58초 수행한 뒤 `Summary bundle invariant fields mismatch in ja`로 fail-closed 종료됐으며 publish·Pages·probe·recovery는 모두 skipped다. validator는 mismatch를 정확히 차단했지만 correction prompt에는 `LOCALE_INVARIANT`만 전달되어 invariant 값과 기대/실제 필드 위치를 잃었다. initial prompt에 각 invariant를 5개 locale의 같은 named field에 두도록 명시하고, correction에는 exact invariant 값·locale·expected/actual field set을 `PREVIOUS_OUTPUT_DIAGNOSTIC_JSON` untrusted data로 격리해 전달했다. 같은 `ja` mismatch가 두 번 이어진 뒤 valid response를 요구하는 RED를 추가했고 전용 correction branch를 무력화한 변이에서 실패함을 확인했다. frozen evidence는 `C:\Users\nasca\AppData\Local\Temp\gh-trending-run-33367283210-35bb462005e34e9e8e3d1ab00ddfd2e5`, snapshot `20260831071138-579622d4ed2937bd`, repositories/readmes `50/50`, `sourceSetSha256=b7a70661e7288bfc6e612385a7ba4ba0123479e00a6e10ec4647afef580176b0`, `runContextSha256=96d42a861fb4e1c50f2e50dff2c00d1168c7fe6c99c13a755421a6fd9a2100e9`, `factsSha256=7e0c8022ee35d4e2d0408bbb53eba119d27524127a3c3d9a08a8c04ee42e9a2e`, `eventsSha256=d14699250664cce01f67fa1ef05efdb6d66b0507805adca3e89313ceb418bad3`이다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 DB·commit·Pages·probe는 0이다. 최종 local 기준선은 Node 522개(510 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

**6단계 최신 후속 — 열네 번째 dispatch의 marketing-field correction 검증 완료**: invariant-field repair는 PR #14와 main `bf5c28f97a577d3ade412e4ef7d06bd078e3a312`에서 PR potential merge와 main 각각 CodeQL 실제 analysis 2건(87·43규칙), results 0, open alerts 0을 통과했다. run `33370143480`은 prepare를 5분 9초에 성공하고 Claude enrichment를 6분 52초 수행한 뒤 `Summary bundle contains unsupported marketing language in en.fit`로 fail-closed 종료됐으며 publish·Pages·probe·recovery는 모두 skipped다. validator와 initial prompt는 홍보성 최상급 표현을 금지했지만 correction은 `UNSUPPORTED_MARKETING`만 전달해 exact locale/field를 잃었다. correction에 `en.fit`과 neutral source-supported rewrite 지침을 전달하는 최소 수정을 적용했다. 같은 invalid `en.fit`이 두 번 이어진 뒤 valid response를 요구하는 RED를 추가했고 전용 branch를 무력화한 변이에서 실패함을 확인했다. frozen evidence는 `C:\Users\nasca\AppData\Local\Temp\gh-trending-run-33370143480-d32005ee4f604503973eaca09c02c6e5`, snapshot `20260831075026-107b468d98479b2a`, repositories/readmes `50/50`, `sourceSetSha256=5dd99a1e5d5d3834947d7c92237c6d574eb29c83bfd134d129125421bde555bb`, `runContextSha256=c8c6528a590dfcc28f24e5b8bfcf4fc637d15cc94b8f076e7b1fb5eca9d7bebc`, `factsSha256=b7db52d910150e242eaeb41360b0c1816d4e3c0d95a8581a83596a0481771e29`, `eventsSha256=d4a787e1a1c8dea2f211a523b2235174728dc385ef0008f3f22e0a2b0067b507`이다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 DB·commit·Pages·probe는 0이다. 최종 local 기준선은 Node 523개(511 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

**6단계 최신 후속 — 열다섯 번째 dispatch의 full invariant audit repair 검증 완료**: marketing-field repair는 PR #15와 main `d8c6cdb717636ba45cff0576800d6b276fb300c0`에서 PR potential merge와 main 각각 CodeQL 실제 analysis 2건(87·43규칙), results 0, open alerts 0을 통과했다. run `33372034170`은 prepare를 5분 56초에 성공하고 Claude enrichment를 7분 47초 수행한 뒤 `Summary bundle invariant fields mismatch in es`로 fail-closed 종료됐으며 publish·Pages·probe·recovery는 모두 skipped다. 이 failure는 기존 invariant-field class의 재발이다. correction이 first mismatch의 exact diagnostic만 강조하면 새 답변의 다른 invariant/locale mismatch가 최대 두 번의 correction 뒤에도 남을 수 있다. 다중 `ko`·`es`·`ja` mismatch fixture로 기존 correction을 RED 고정하고, diagnostic 1건뿐 아니라 새 답변의 모든 declared invariant를 5개 locale 전체에서 English와 exact same named fields인지 audit하도록 지시했다. audit 문구를 제거한 변이는 다시 실패했다. frozen evidence는 `C:\Users\nasca\AppData\Local\Temp\gh-trending-run-33372034170-d8cb12c7b8ce4ecd873510e924cb4cdb`, snapshot `20260831081522-b20916edf455768c`, repositories/readmes `50/50`, `sourceSetSha256=511fa94330d4d1613ca70db61aadaf2e5c7a0269514a17b0bc9df84f0f51c801`, `runContextSha256=d7fa0d278177752d5e3acbc43d4761621a4bed28255c6d0e07639eec1f0b6c5b`, `factsSha256=b42d772acfd52698d084fd76a3d28ccf9a13deb578d4cd805e4e9b29eb1bf3e2`, `eventsSha256=d075f460adbd4b5eaff3133209d689ba4d20f8d8309df638880f7b6405261b20`이다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 DB·commit·Pages·probe는 0이다. 최종 local 기준선은 Node 524개(512 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

**6단계 최신 후속 — 열여섯 번째 transient collector 실패와 열일곱 번째 rendered-Markdown invariant repair 검증 중**: full-invariant repair는 PR #16과 main `80f3e6cccee30a8c2a4372eb0deb33697ea86600`에서 CodeQL 실제 analysis 2건(87·43규칙), results 0, open alerts 0을 통과했다. run `33374144042`은 세 번째 active repository `Lakr233/vphone-cli`의 `GitHub metadata unavailable`로 frozen facts 수집에서 종료됐고 Claude·DB·commit·Pages·probe는 0이다. 같은 repository의 metadata·contributors·HEAD·canonical README blob·variant tree 전 경로를 현재 다시 호출해 모두 정상임을 확인한 뒤 같은 SHA를 한 번 재실행했다. run `33375183865`은 prepare를 5분 11초에 성공하고 Claude enrichment를 8분 54초 수행한 뒤 `Summary bundle invariant is absent from README: Node.js >= 20`으로 fail-closed 종료됐다. artifact의 `THU-MAIC/OpenMAIC` README는 `**Node.js** >= 20`을 exact source로 가지므로, 사실 불일치가 아니라 raw Markdown strong-emphasis delimiter와 렌더된 summary text의 표현 차이다. version·number·product invariant만 raw exact match 우선 후 candidate 전체 또는 단일 token이 `**…**`·`__…__`으로 감싸진 bounded raw variant에도 묶도록 최소 수정하고 command·URL은 기존 raw exact 계약을 유지한다. `Node.js >= 20`은 통과하고 `Node.js >= 21`은 계속 거부하는 RED를 추가했으며 version 보조 비교를 제거한 변이는 다시 실패했다. frozen evidence는 `C:\Users\nasca\AppData\Local\Temp\gh-trending-run-33375183865-a7c2c5a62439463ab23ef22061f42471`, snapshot `20260831085542-8779ce35aed904eb`, repositories/readmes `50/50`, `sourceSetSha256=dfe16bca0b07f36edf8282831289b12c8144a21f04660321434ddb317573546b`, `runContextSha256=641d207b056a01d1bec78482f5538ae3cada761b0846d3d708f980bb5a11e814`, `factsSha256=1b26cdc9882f397a048db3f3bf8e4d53e00404c20f418e92ab104bfe3a7c5b6c`, `eventsSha256=327a053757272085f7de42a872faab500f89eeff68bd7726d838b7df60bb35dd`, `completeSetSha256=22acb738c5c82166b9e3af47d19ae911b7aa53dd41e1c4ac7003ee8ce1f249c6`이다. usage receipt 전 실패해 exact calls/tokens는 `unknown`이고 publish·DB·commit·Pages·probe는 0이다. 최종 local 기준선은 Node 525개(513 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

**6단계 최신 후속 — 열여덟 번째 dispatch와 producer/validator 인터페이스 개편 검증 완료**: rendered-Markdown repair는 PR #17과 main `aca6b975a8c97f992edae6b26ae22e85931516f6`에서 CodeQL 실제 analysis 2건(87·43규칙), results 0, open alerts 0을 통과했다. run `33378871320`은 prepare와 실제 Claude enrichment를 정상 실행한 뒤 `Summary bundle invariant is absent from en summary fields`로 fail-closed 종료됐고 publish·DB·commit·Pages·probe는 0이다. 최근 변경 이력을 넓게 재검토한 결과 `scripts/generate-summary-bundles.mjs`에 evidence·invariant·inference·marketing 결함별 반응형 보정이 연속 누적됐지만, validator는 첫 결함에서 멈추고 correction은 직전 응답 없이 5개 언어 전체를 새로 생성해 다른 결함을 다시 만들 수 있었다. producer와 validator 사이의 deterministic adapter를 깊게 만들어 README inventory 중 실제 영문 summary에 쓰이지 않은 invariant는 저장 전에 제거하고, summary·evidence·invariant·inference의 독립 결함을 한 번에 수집하며, correction에는 해시·길이로 경계 지은 직전 출력과 전체 defect 목록을 전달해 결함 항목만 수정하도록 변경했다. terminal failure에는 exact repository slug와 defect count를 남긴다. unused-invariant fail-closed와 first-defect-only 및 previous-output 제거 변이는 각각 새 RED를 다시 실패시켰다. 최종 local 기준선은 Node 527개(515 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint pass, production dependency audit 0이다.

대안 비교에서는 model-declared invariant를 전부 없애는 방식은 product name의 exact cross-locale 계약을 약화하므로 제외했고, English generation과 localization을 두 번의 호출로 나누는 방식은 호출 수·실행시간·새 실패 seam을 거의 두 배로 늘려 제외했다. 선택한 adapter 방식은 기존 단일 atomic bundle과 stored schema를 유지하면서 모델이 중복 선언한 값만 정규화하고 deterministic validator의 진단 깊이만 높인다.

**6단계 최신 후속 — 열아홉 번째 dispatch와 targeted correction 인터페이스 검증 완료**: producer/validator adapter repair는 PR #18, main `7ee589c2d2952f51db50d1e9e615dac0455ea2b3`, CodeQL run `33382835965`의 실제 analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. run `33383238462`은 prepare를 4분 59초, 실제 Claude enrichment를 14분 27초 수행한 뒤 `Summary bundle contains a generic or placeholder es.cons [repository=Lakr233/vphone-cli; defects=5]`로 fail-closed 종료됐으며 publish·DB·commit·Pages·probe·recovery는 모두 skipped다. frozen input은 snapshot `20260831103722-c4820a9a5c135355`, repositories/readmes `50/50`, `activeSetSha256=99645392adda6dde6cbf12bcbaf22eaad11e34e41a130ac97121c8a74ec048e2`, `sourceSetSha256=5f970fef7714b488bab0e9167b95be87f3054d7943e667938a8ec4344c3ebdce`, `runContextSha256=c2ff2df5230a967a8ccf0df8b1458eac6d910b4556bedba9d0b8b047480a13b0`, `factsSha256=bcdc72a3e8c259d2050d337eb5383bad30ddf6297c059a1ca6c95d439ffa8bf3`, `eventsSha256=e4ea7da9d9ad569ccd6669d5629eecebfdf628b6eb3785f6cef852b4efa9d8b7`다. 실패 당시 artifact에는 frozen input만 있고 모델 출력·전체 defect 목록·usage receipt가 없어 exact 5개 defect와 calls/tokens는 `unknown`이다.

반복 원인을 단일 Spanish 문구가 아니라 correction Interface 전체에서 재검토했다. 첫 응답은 5 locale × 5 field 전체 bundle이 필요하지만 correction도 같은 전체 schema를 사용해 이미 정상인 필드를 다시 생성했고, 한 필드의 fallback이 generic·declared invariant field·cross-locale token 결함을 동시에 만들 수 있었다. correction을 validator-selected patch schema로 교체해 `summaries.es.cons` 결함이면 모델도 그 경로 하나만 반환할 수 있게 하고, evidence·invariants·inference도 결함 종류별 최소 replacement만 허용한다. 1차 patch의 부분 성공은 다음 correction의 immutable base로 유지하며, terminal failure는 모델 출력이나 README 본문 없이 defect code·locale·field·invariant digest·usage·runtime만 `refresh-enrichment-failure-<run_id>` artifact로 남긴다. 전체 schema 복귀, 부분 성공본 폐기, defect artifact 제거 변이는 각각 새 RED를 실패시켰다. 최종 local 기준선은 Node 531개(519 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스무 번째 dispatch의 cross-locale token metadata repair 검증 중**: targeted-correction repair는 PR #19, main `208f633eced8b7d1753ef73bed255b384b3a3e74`, main CodeQL run `33387998370`의 실제 analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. run `33388405416`은 prepare를 2분 55초에 성공하고 실제 Claude enrichment를 16분 49초 수행한 뒤 `Summary bundle cross-locale invariant mismatch in goal [repository=THU-MAIC/OpenMAIC; defects=6]`로 fail-closed 종료됐다. 새 bounded failure artifact `refresh-enrichment-failure-33388405416`은 원문 summary/README 없이 `ko`·`zh-CN`·`ja`의 `goal`·`usage` 6개 token mismatch와 usage `inputTokens=745890`, `outputTokens=217959`, `attempts=17`, `retries=11`, Claude CLI `2.1.241` OAuth first-party Sonnet 5 runtime을 보존했다. enriched output은 skipped였고 publish·DB·commit·Pages·probe·recovery는 모두 0이다. frozen input은 snapshot `20260831114500-20a557ee5d887385`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=fd493cf64ba0a3e17b9005fb8827adb3931ac2ea8b70b929f49fae0f8c0c832e`, `runContextSha256=212318cc614855eb27e7e4168bc6b210c851b6d08c7a1e979994e081c503a957`, `factsSha256=70b097da4717cf8ce2e709dabe518bc9487b46faa883307106e7766e3ab2c837`, `eventsSha256=6ba432fb91bb94ffc0493176de33276f49da9f59d59d3e1d3527376041f6ee1d`다.

targeted schema는 정확한 6개 경로를 선택했지만 validator의 `expected`/`actual` command·URL·number inventory가 기존 defect serializer에서 빠져 correction에는 `LOCALE_INVARIANT`와 locale/field만 전달됐다. 모든 quality defect가 단일 canonical serializer를 거치도록 바꾸고, correction prompt에는 exact `expected_tokens`/`actual_tokens`를 untrusted data로 전달하며 failure artifact에는 token 원문 대신 mismatch 종류와 expected/actual 개수만 남긴다. `ko`·`zh-CN`·`ja`의 `goal`·`usage` 6개 mismatch fixture에서 serializer를 제거하면 새 RED가 실패함을 확인했다.

**6단계 최신 후속 — 스물한 번째·스물두 번째 dispatch와 source-grounded marketing false positive 수정 검증 완료**: cross-locale token repair는 PR #20, main `0205981bd9f9882b6dd67ac323cc00bac9122d6f`, main CodeQL run `33391685789`의 actual analysis 2건(Python 43규칙, JavaScript/TypeScript 87규칙), results 0, open alerts 0을 통과했다. run `33392159705`는 verified-v0 manifest가 HTTP 200인 상태에서 operator가 불필요한 `bootstrap-source-sha`를 전달한 호출 오류로 prepare 40초에 fail-closed 종료됐다. enrich·publish·deploy·probe·recovery는 전부 skipped, artifact 0이며 production은 version 0, source `0aa617016c0e832909f74e3d9a70bbe210c10d60`, manifest SHA-256 `fb3e6ae40c8ba30a05d899790fb3cd112b6635db9ae510101a4d633cc7b49c21`, 51파일로 불변이다.

새 사용자 승인 뒤 inputless로 실행한 run `33392487673`은 prepare를 3분 43초에 통과하고 실제 Claude enrichment를 11분 11초 수행한 뒤 `checkstyle/checkstyle`의 `en.goal`, `zh-CN.goal` 2개 `UNSUPPORTED_MARKETING` defect로 종료됐다. bounded diagnostic은 usage `inputTokens=720570`, `outputTokens=193896`, `attempts=19`, `retries=11`, Claude CLI `2.1.241` OAuth first-party Sonnet 5 runtime을 보존했고 publish·DB·commit·Pages·probe·recovery는 모두 0이다. frozen input은 snapshot `20260831123514-be1274d3cbc2f06f`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=aa57992d39a5701644b627b1d4cede821d4b5af4bffd2352349413ca6be74a81`, `runContextSha256=86076b2be32181d2d06a045d9c651f7e8cb20ea7027417e228e40b84e7878c85`, `factsSha256=3eadf5c00461c2527549d15dd9d9e9ce3d5d5fa3907a25039aeca1fad8a72d6f`, `eventsSha256=e448bf29c5df21869fcc189ce6b085910c259c125052e7dafdec0ccbebf6e76f`다.

exact frozen README blob `cb101acfc8dd801a7ad1f1c7b8c81226b54abcbb`와 content SHA-256 `8a31fca0bd8f7278391e65b37cf4ded8c9166ef31d3992194448b027ac2f3961`을 재조회한 결과 원문 7행은 Checkstyle을 code standard 또는 `best practices` 준수 도구라고 설명한다. 기존 validator는 중립적인 기술 용어 `best practices`의 `best`와 그 중국어 번역 `最佳实践`의 `最佳`까지 promotional superlative로 거부해, source-grounded 요약 지시와 정규식이 충돌했다. `best practices`·`最佳实践`만 기술 용어 예외로 두고 실제 금지 매치에는 `forbidden_terms`를 correction defect와 bounded failure artifact에 전달한다. RED fixture는 run과 같은 en/zh-CN 두 defect를 재현했고 수정 후 GREEN이다. 별도 agent가 제시한 runtime 가설 중 현재 원인이 아닌 OAuth·후처리 문제는 actual token usage와 실패 위치로 배제했지만, latent post-success 위험인 OAuth log mask, missing `translations` directory, reused temp data directory, repository slug의 `cost` 문자열 오탐은 workflow fixture와 함께 보강했다. 최종 local 기준선은 Node 533개(521 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스물세 번째 dispatch의 transient Pages probe retry 수정 검증 중**: source-grounded marketing repair는 PR #21, main `a37f56d3eee0c23c72c88325f3aa7986163e11e9`, main CodeQL run `33395535215`의 actual analysis 2건(Python 43규칙, JavaScript/TypeScript 87규칙), results 0, open alerts 0을 통과했다. run `33395938647`은 inputless production-state 검증에서 `readmes/block__buzz.md`에 대한 GitHub Pages CDN HTTP 503 한 건을 만나 prepare 48초에 fail-closed 종료됐다. enrich·publish·deploy·probe·recovery는 전부 skipped, artifact 0, provider 0회이며 production은 verified v0로 불변이다. 역추적 결과 `probe-production.mjs`의 `fetchBytes`가 transport/503 재시도 없이 즉시 종료하는 것이 안정 실행 차단 원인이었다. 404·redirect·body-size·hash mismatch는 계속 즉시 실패시키고 408·425·429·500·502·503·504와 transport 오류만 250ms·1s bounded delay로 최대 2회 재시도한다. legacy Pages server가 첫 README 요청에 503을 반환하고 두 번째에는 exact bytes를 반환하는 RED fixture는 수정 전 실패하고 수정 후 통과했다.

**6단계 최신 후속 — 스물네 번째 dispatch의 exact invariant action correction 검증 완료**: transient Pages probe repair는 PR #22와 main `b90b4b55a513f8611619b6162ac14f2a92861690`, CodeQL run `33397006236`의 actual analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. inputless run `33397406754`은 prepare를 2분 51초에 성공하고 실제 Claude enrichment를 4분 16초 수행한 뒤 `K-Dense-AI/scientific-agent-skills`의 Spanish `fit`에서 4개 defect로 fail-closed 종료됐다. bounded diagnostic은 `Cursor`, `Claude Code`, `Codex`의 invariant digest 3개가 English의 expected `usage` 외 Spanish `fit`에도 중복됐고, 같은 `fit`의 number token count가 expected 2개에서 actual 3개로 늘었음을 보존했다. usage는 `inputTokens=310899`, `outputTokens=56456`, `attempts=7`, `retries=4`, runtime은 Claude CLI 2.1.241 OAuth first-party Sonnet 5다. frozen input은 snapshot `20260831133115-ebf82bc481fbd405`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=81628afe0a7f1d7192e6fad9c7ff85eec8f30edd990de9f715182c058cba99bc`, `runContextSha256=95ad401d0ea45203c451e59ea062c1b2af2ca614330945c2d6d3191af39bd8b7`, `factsSha256=35408803b49ecca1ba91807374ca440d6b69cc6fccced866af742bfaa641058c`, `eventsSha256=87e9960c3add796d581f5204c6b4f69ebf8266326a928566765426d32068ddaf`다. exact README provenance는 blob `4e15094f3ae31b9b33ccef1c7abd7e587be68ea5`, content SHA-256 `d8b4d2e0aa33f8c08c0b1fe82d2a1a780dc608fe5d9f1ec8baff5fb33fc04166`, head `cc37669ed0f354619b1ae586e958609a87680718`이다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0는 불변이다.

validator-selected schema는 정확히 `summaries.es.fit`만 다시 생성하도록 제한했고 correction에도 exact expected/actual set을 전달했지만, “same named fields에 보존”이라는 추상 지시만으로는 두 번의 correction이 extra product와 number를 제거하지 못했다. defect serializer에 exact `add_to_fields`·`remove_from_fields`를 추가하고 correction은 `remove exact invariant "Cursor" from es.fit` 같은 실행 지시와 exact `expected_tokens` inventory replacement를 전달한다. LLM 결과를 후처리로 자동 변조하지 않고 partial correction의 immutable base와 모든 deterministic validator를 유지한다. run과 같은 extra product·number fixture는 수정 전 RED, 수정 후 GREEN이며 remove-target 계산을 비운 변이는 다시 실패했다. 최종 local 기준선은 Node 534개(522 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스물다섯 번째 dispatch의 correction prompt ordering repair 검증 중**: exact invariant action repair는 PR #23과 main `426417ed9cbc157c45b00ec57e91cbee9ed7ad52`, CodeQL run `33399922452`의 actual analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. inputless run `33400238848`은 prepare를 2분 55초에 성공하고 실제 Claude enrichment를 11분 수행한 뒤 `majd/ipatool`의 Korean·Simplified Chinese·Spanish·Japanese `pros`에서 같은 `auth` invariant가 누락된 4개 defect로 fail-closed 종료됐다. English expected fields는 `usage`, `pros`이고 네 locale의 actual fields는 `usage`뿐이었다. bounded diagnostic usage는 `inputTokens=605334`, `outputTokens=162623`, `attempts=16`, `retries=9`, runtime은 Claude CLI 2.1.241 OAuth first-party Sonnet 5다. frozen input은 snapshot `20260831140151-363ee9d238ac35c3`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=5b39c9b5cf2a303b1d85d195b3f148d450bdd1a3e78921fe9356381cae2806bd`, `runContextSha256=f644ac56a0a333c0d1dc80bc6e7ac48cfc3abe790ae79a36b1385409ef897585`, `factsSha256=a4386716809622aba69b8acafd115e4d5d5a647956f28e2b3b58314a4153f16b`, `eventsSha256=d10b9da79ee49084425cf9cc018b8d8265c9121283fcc646c81d3e22b5fc4ec0`이다. exact README provenance는 blob `63200f00528c144cfe8b1e585c3dc75ca09983b7`, content SHA-256 `fb068d4618d6f94525f95630c95c18500e084a7f37f35695df04353d08eca900`, head `d5d0b56faf64e3fdef885d49e7928b390aadb6c7`이다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0는 불변이다.

run #25 correction에는 새 exact `add_to_fields`가 이미 있었지만 두 번 모두 같은 4개 누락을 남겼다. correction prompt 전체를 역추적하니 validator feedback과 target instruction 뒤에 최대 24,000자의 `PREVIOUS_OUTPUT_JSON`이 오고 prompt가 그대로 끝나, 최종 수정 명령이 긴 untrusted 출력 앞에 묻히는 순서였다. prompt를 `PREVIOUS_OUTPUT_JSON`과 명시적 `END_PREVIOUS_OUTPUT_JSON` 경계로 먼저 닫고 그 뒤에 exact validator feedback·correction targets·반환 계약이 마지막으로 오도록 재배치했다. run과 같은 4-locale `auth` addition fixture와 앞선 extra-product removal fixture는 마지막 instruction 구간을 직접 검사한다. 예전 순서로 되돌린 변이에서는 둘 다 실패했고 원복 후 GREEN이다. 최종 local 기준선은 Node 535개(523 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스물여섯 번째 dispatch의 schema-bound field requirements 검증 중**: correction ordering repair는 PR #24와 main `f94cec5098cab4f548030582e33af8dfc7244112`, CodeQL run `33402807346`의 actual analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. inputless run `33403148891`은 prepare를 3분 11초에 성공하고 실제 Claude enrichment를 15분 22초 수행한 뒤 `handsomestWei/patent-disclosure-skill`의 2개 defect로 fail-closed 종료됐다. Japanese `goal`에는 English expected inventory에 없는 number token 1개가 추가됐고 Korean `fit`은 `inference_fields`에 포함됐지만 explicit hedge가 없었다. bounded diagnostic usage는 `inputTokens=929374`, `outputTokens=292535`, `attempts=27`, `retries=12`, runtime은 Claude CLI 2.1.241 OAuth first-party Sonnet 5다. frozen input은 snapshot `20260831143145-a2d4170188852a8b`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=399b22bdfeb2d86efb326cb3e8ac1f8fc453d7a4407362cf969aa1ee7d8585cc`, `runContextSha256=1cd24f5f64ec0a00c74ec9190618bc92351fd23bf5d87eab66a5b78009609eac`, `factsSha256=de17e8d13f6c94addc7f2b1ca2a128b300e3a35dacd71e4bff1c9b43449e6079`, `eventsSha256=cc842e1488a4594cb5d4b84fa566c021ec17bc6ccad87f54fb9157e6e1e32f65`다. exact README provenance는 blob `60f13496858063beeef24bcdd8eb5d1b908e0b32`, content SHA-256 `709795ac5a81426633a335a274c636bea4c439bb096d1ee8402a6c3c5ac6547d`, head `ecd62fdb45b9792bb5fb2ebe8dc61157e04faab0`이다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0는 불변이다.

exact correction feedback가 prompt 마지막에 도달해도 correction JSON schema의 selected string fields에는 type·minLength·maxLength만 있었고 path별 validator requirement는 없었다. `correctionFieldDescriptions`가 quality defects를 exact selected path에 다시 매핑해 `ja.goal` schema에는 expected/actual token inventory requirement를, `ko.fit` schema에는 locale-specific explicit hedge requirement를 description으로 결합한다. JSON schema shape, validator, retry cap 12, 호출 수는 바꾸지 않는다. run과 같은 combined token/hedge fixture는 수정 전 RED, 수정 후 GREEN이며 description 결합을 제거한 변이는 다시 실패했다. 최종 local 기준선은 Node 536개(524 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스물일곱 번째 dispatch의 schema-enforced inference hedge 검증 중**: schema-bound field repair는 PR #25와 main `537fa60a2ef9123d623eef71360c3f609653d7f5`, CodeQL run `33407158067`의 actual analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. inputless run `33407705006`은 prepare를 3분 1초에 성공하고 실제 Claude enrichment를 5분 41초 수행한 뒤 `K-Dense-AI/scientific-agent-skills`의 Spanish `cons`에 explicit inference hedge가 없는 `OUTPUT_SCHEMA` 1건으로 fail-closed 종료됐다. bounded diagnostic usage는 `inputTokens=397453`, `outputTokens=87368`, `attempts=10`, `retries=6`, runtime은 Claude CLI 2.1.241 OAuth first-party Sonnet 5다. frozen input은 snapshot `20260831151845-6d75dcd6ba6fc5c3`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=62708cb40cb2b00c0e031458f83f36375b211d55a1d7c7f70011429980d56f24`, `runContextSha256=e743d5d79c9e5278f088286d19d584392323fabfb9b17ccc748d4f8b6616c3b3`, `factsSha256=2a8ac71bf1bcd991cfe40f53a3644b639647d7bd29d36e7780b779d278fc8bbf`, `eventsSha256=3887f1d720b6a2479489797450312ae9637cd9fac486e2842077adb3708268af`다. exact README provenance는 이전과 같은 blob `4e15094f3ae31b9b33ccef1c7abd7e587be68ea5`, content SHA-256 `d8b4d2e0aa33f8c08c0b1fe82d2a1a780dc608fe5d9f1ec8baff5fb33fc04166`, head `cc37669ed0f354619b1ae586e958609a87680718`이다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0는 불변이다.

run #27은 exact hedge instruction이 prompt 끝과 selected field description에 있어도 모델이 이를 지키지 않아, 자연어 설명을 JSON schema 강제로 오인한 설계 한계를 드러냈다. validator가 쓰는 다섯 locale hedge 정규식을 단일 `HEDGE_SCHEMA_PATTERNS` 정본으로 만들고, `OUTPUT_SCHEMA` correction target에만 같은 regex를 structured-output `pattern`으로 결합한다. Claude의 constrained structured output이 이제 hedge marker 없는 correction string을 생성할 수 없으며, summary 문장 사후 변조·validator 완화·retry cap·호출 수 변경은 없다. run과 같은 `es.cons` fixture는 pattern 부재에서 RED, 결합 후 GREEN이며 pattern 제거 변이는 다시 실패했다. 최종 local 기준선은 Node 537개(525 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass, production dependency audit 0이다.

**6단계 최신 후속 — 스물여덟 번째 dispatch의 Claude request diagnostic 소실 복구 검증 중**: schema-enforced hedge repair는 PR #26과 main `1c844583ab8df966b275261a000f072c24b90dea`에서 PR potential merge와 main 각각 CodeQL 실제 analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, open alerts 0을 통과했다. inputless run `33410426347`은 prepare를 3분 8초에 성공하고 실제 Claude enrichment를 6분 4초 수행한 뒤 `K-Dense-AI/scientific-agent-skills`에서 `CLAUDE_REQUEST_FAILED`, defects 0으로 fail-closed 종료됐다. bounded diagnostic usage는 `inputTokens=263460`, `outputTokens=87723`, `attempts=9`, `retries=5`, runtime은 Claude CLI 2.1.241 OAuth first-party Sonnet 5다. frozen input은 snapshot `20260831154717-e6e3be3fdb2ddfc1`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=dc02bc243c51214e65dbad2013679b6d64313f5f1eb9128dde4519a2208e7e3f`, `runContextSha256=e2a8deb7835654a67139e92d8514952497e342e45f8899974fb1b5cc29d682d9`, `factsSha256=c9661d6cf7b029e646e63aff2912563ca46e44de94fc04ca77ec9c33b5cb5748`, `eventsSha256=efbd845e4ebe039adb4308c103b0ae3550fb60d0bac0763f93f17d8cf328e78c`다. exact README provenance는 blob `4e15094f3ae31b9b33ccef1c7abd7e587be68ea5`, content SHA-256 `d8b4d2e0aa33f8c08c0b1fe82d2a1a780dc608fe5d9f1ec8baff5fb33fc04166`, head `cc37669ed0f354619b1ae586e958609a87680718`이다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0는 불변이다.

같은 frozen K-Dense 입력을 현재 producer로 단독 재생한 결과 initial structured call과 `pattern`이 결합된 correction call이 모두 성공했다(`inputTokens=99584`, `outputTokens=20192`, logicalCalls 1, attempts 2, retries 1). 따라서 OAuth User scope, structured-output `pattern`, correction schema 복잡도, 해당 README 자체의 결정적 실패는 배제됐다. 기존 runtime이 bounded stderr/stdout을 재시도 판정에만 사용한 뒤 모든 non-quality 종료를 `CLAUDE_REQUEST_FAILED`로 축약해 원래 category를 잃은 것이 확인됐다. 원문 진단은 저장하지 않고 schema invalid, auth, rate limit, transient provider, timeout, output limit, process execution, unknown request의 고정 allowlist code만 error와 failure artifact로 전달한다. 기존 transient regex와 retry 횟수는 유지한다. timeout/schema/auth/429/503/unknown RED와 artifact propagation RED는 수정 후 GREEN이며 classifier와 propagation을 각각 무력화한 변이는 다시 실패했다. 최종 local 기준선은 Node 539개(527 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass(custom self-hosted label 한 항목만 exact ignore), production dependency audit 0이다.

**6단계 최신 후속 — 스물아홉 번째 dispatch와 동일 frozen input 전체 재현·renderer 계약 복구 검증 완료**: request diagnostic repair는 PR #27과 main `e44dae9f101ca797aaa7ed94a0706e1e03772372`에서 exact CodeQL analysis를 통과했다. run `33414846737`은 snapshot `20260831163430-8b2c2f0812274099`, repositories/readmes `45/45`, `sourceSetSha256=905cff0c2bf115462f53dcf6b9563245ea97f0e53e24849dd2b3151492113a0a`, `runContextSha256=4169800691b252a91724eb85a7c5e9266ed95cf41ec2182d2bbc5eecc6d35c78`에서 `K-Dense-AI/scientific-agent-skills`의 allowlisted `CLAUDE_REQUEST_FAILED`로 fail-closed 종료됐다. bounded usage는 `inputTokens=291075`, `outputTokens=83747`, `attempts=9`, `retries=5`이고 publish·DB·commit·Pages·probe는 모두 0이다. 같은 frozen input을 안전 계측한 현재 producer로 45개 전부 재현한 결과 Claude CLI OAuth 호출은 transport/auth/rate-limit/timeout 오류 0건으로 완주했다. 영수증은 logicalCalls 45, attempts 98, retries 53, inputTokens 3526556, outputTokens 1131256, runtime Claude CLI 2.1.241 OAuth first-party Sonnet 5다. 이 재현은 OAuth·PowerShell·stdout 계층이 현재 blocker라는 가설을 반증하고, 실제 불안정점이 deterministic semantic quality correction에 있음을 확정했다.

전체 재현 중 `Osmantic/ODS`는 source-language phrase `2 minutes`를 number invariant로 선언해 번역 locale에 영어 원문 삽입을 요구받는 계약 충돌을 드러냈다. number invariant를 숫자·버전·단위가 붙은 canonical token으로 제한하고 잘못된 선언은 summary field가 아니라 invariant declaration만 교정하도록 바꿨다. `K-Dense-AI/scientific-agent-skills`, `p-e-w/heretic`, `ConardLi/garden-skills` 등은 초기 생성과 두 번의 correction 이후에도 한 semantic defect가 남아 기존 한도를 소진했다. normal cap 12는 유지하고 최초 승인 bootstrap만 pending 45개에 대해 최대 135회의 유한 correction capacity를 가지며, repository별 initial + 최대 3회 targeted correction으로 제한했다. generic/marketing validator는 검출한 exact phrase만 `forbidden_terms`로 전달하고 model output 전체는 failure artifact에 남기지 않는다.

LLM 45개 생성이 끝난 뒤 첫 exact coverage는 `Summary bundle source coverage is invalid for THU-MAIC/OpenMAIC`로 RED였다. 역추적 결과 `update-trending.mjs#createPageSnapshot`이 v1 cache의 `summaries/evidence/invariants/inference_fields`를 렌더 중 제거하고 `content/source`만 남기는 producer→renderer 계약 단절이었다. v1 entry에 이미 존재하는 네 필드만 그대로 보존하고 legacy/synthetic entry에는 provenance를 만들지 않도록 최소 수정했다. 같은 frozen candidate를 재렌더한 exact coverage는 repositories 45/45, locales 225/225, missing 0, stale 0, insufficient_source 0, translations 0으로 통과했다. 3차 correction, invariant declaration routing, renderer bundle preservation을 각각 제거한 의도적 변이는 모두 RED를 재현했다. 최종 local 기준선은 Node 542개(530 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass(custom self-hosted label 한 항목만 exact ignore), production dependency audit 0이다.

**6단계 최신 후속 — 서른 번째 dispatch의 Spanish `todo` false-positive 복구 검증 완료**: bootstrap stability repair는 PR #28과 main `03247805ca6520da34961d616d9c2dbf88fe586f`에서 exact CodeQL analysis를 통과했다. inputless run `33429148032`은 prepare를 3분 29초에 성공하고 실제 Claude enrichment를 약 15분 수행한 뒤 `affaan-m/ECC`의 `es.cons`에서 `GENERIC_OR_PLACEHOLDER`, forbidden term `todo` 1건으로 fail-closed 종료됐다. bounded diagnostic usage는 `inputTokens=1252964`, `outputTokens=295987`, `attempts=29`, `retries=16`이다. frozen input은 snapshot `20260831191109-1db511c4fea130a0`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=64992d80c1a9f61f1ff59ea81aee9ea74a3fdeead0db8afd9e2c46737c418bf1`, `runContextSha256=91ca8411aba9af1ed6299b2b9a39fd02869cf0aa178223806cc6cff73be22680`, `factsSha256=fcd68b0986b758ecd5ee5cdb58831c36d04e454422080297ab065a2e747ee7fd`, `eventsSha256=ba96959ab5fc51b54361b9b7070b1ed49d32afe7b8a83be1ca5a92847730df`다. affaan README provenance는 path `README.md`, blob `00d3a8ca7e6a9d602085fe9f9eebaa37e23513af`, content SHA-256 `9eed47de173c23b85f04d82f2167991ba466b2cc5feb59a49557fbcc7b973543`, head `005eff40fd4a4ac005da7a70e713459175385516`이며 publish·DB·commit·Pages·probe는 모두 0, production verified v0는 불변이다.

원인은 placeholder marker 정규식의 case-insensitive flag가 스페인어 일반 단어 `todo`(모든/전체)를 대문자 작업표시 `TODO`와 동일하게 취급한 것이었다. `TODO`·`TBD`만 case-sensitive marker로 분리하고 나머지 다국어 generic 문구의 case-insensitive 검사는 유지했다. 소문자 Spanish prose 허용과 대문자 `TODO` 거부를 한 테스트로 RED→GREEN 고정했으며 marker에 `/i`를 되돌린 변이는 다시 RED였다. 같은 frozen affaan 입력의 격리 재생은 logicalCalls 1, attempts 2, retries 1, inputTokens 176772, outputTokens 17497로 성공했다. 최종 local 기준선은 Node 543개(531 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass다.

**6단계 최신 후속 — 서른한 번째 dispatch의 product invariant field-distribution 계약 복구 검증 완료**: Spanish `todo` repair는 PR #29와 main `1d9f85f4d0de31db9bbb296e59ec69b66cce64d2`에서 PR potential merge와 main 각각 exact CodeQL analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, warning/error 0, open alerts 0을 통과했다. inputless run `33433157231`은 prepare를 3분 35초에 성공하고 실제 Claude enrichment를 42분 19초 수행한 뒤 `pingdotgg/t3code`의 `es`에서 product invariant field mismatch 1건으로 fail-closed 종료됐다. bounded usage는 `inputTokens=2755861`, `outputTokens=843710`, `attempts=84`, `retries=43`이다. frozen input은 snapshot `20260831195509-83b8a5ccf2c30452`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=77307cf544560b7d04756ef3c1de3e79ecc402cb88ff93550a19c89ecd405bf8`, `runContextSha256=60c44c2198c94fc7a6a5c7e92e4f3d47f89898f243572fb6337d7e75a8109861`, `factsSha256=133a77185a057b2b0989bc08ca9733525d648236cae65799e07a6079e88c4f14`, `eventsSha256=7d4980ee4121d4576634f227d25f27d136ef4eb6b559fba6fb23301f0e71cea4`다. exact t3code README provenance는 path `README.md`, blob `8ec101387f678291417d28608338c4cd582f5dad`, content SHA-256 `c1079a3a7c0e58a88f4342dacbee525e0e24cbed61f71f0199fef2ffe0bca482`, head `31c1c5996f88e3acf1566adc11c9b51ac7561554`다. publish·DB·commit·Pages·probe는 모두 0이며 production verified v0와 manifest SHA-256 `fb3e6ae40c8ba30a05d899790fb3cd112b6635db9ae510101a4d633cc7b49c21`은 불변이다.

product invariant repair는 PR #30과 main `15837bf3d780e15d643fdd6d857e536bd4cebd4e`에서 PR potential merge와 main 각각 exact CodeQL analysis 2건(JavaScript/TypeScript 87규칙, Python 43규칙), results 0, warning/error 0, open alerts 0을 통과했다.

**6단계 최신 후속 — 서른두 번째 dispatch의 실제 Claude subscription rate limit과 v0 재시작 증폭 복구 검증 중**: run `33440722982`은 prepare를 3분 14초에 성공하고 실제 Claude enrichment를 약 33분 수행한 뒤 `AprilNEA/OpenLogi`에서 `CLAUDE_RATE_LIMITED`, defects 0으로 fail-closed 종료됐다. bounded usage는 `inputTokens=2332790`, `outputTokens=663554`, `attempts=74`, `retries=42`이며 publish·DB·commit·Pages·probe·recovery는 모두 skipped다. frozen input은 snapshot `20260831211913-5f3e33c0e96f3798`, repositories/readmes `45/45`, `activeSetSha256=cb8f9e2d3a700ce93a0d44d8cda0ae10f677233f6121b62b8dea5c98a226111b`, `sourceSetSha256=14e9b2dfd8da5ed01b28e5af50ad146b6ff1181874fb141797ea34ec1e1f4d96`, `runContextSha256=cf125e651748947d8bb48f92cb24717cb26c5d4916f3dd67091579f36427f19c`, input source `15837bf3d780e15d643fdd6d857e536bd4cebd4e`, hydration source `0aa617016c0e832909f74e3d9a70bbe210c10d60`이다. 보존한 artifact의 raw file SHA-256은 facts `671ab18e5c3000cafdbaf7745b0801cafe5a6aca0400df070321a3263a67cd07`, events `56bbec847909ba015be8906586dc4b255b50d3cdec64ce8d1d07ef8476be7d05`다. 별도 bounded 진단과 seeded replay는 HTTP 429 marker를 확인했고 첫 pending repository에서 `inputTokens=0`, `outputTokens=0`, `attempts=9`, `retries=6`으로 같은 `CLAUDE_RATE_LIMITED`를 재현했다. 따라서 OAuth User scope, PowerShell 문법, stdout 영수증 오염, semantic validator가 이 failure의 원인이라는 가설은 현재 증거와 모순된다. reset 시각은 진단에서 노출되지 않아 `unknown`이고, 외부 한도가 바뀌기 전 blind dispatch는 하지 않는다.

현재 producer는 모든 pending repository가 끝난 뒤에만 candidate cache를 기록하므로 한 번의 terminal rate limit이 앞서 검증된 결과까지 버리고 다음 v0 run에서 45개 전체를 다시 호출하는 재시작 증폭이 있었다. run `33414846737`의 동일 45-repository 전체 재현 cache(`SHA-256=1408963c57edac1a79783ce7cf40e39ff294ef80cab2c7d3264d98f7005ed640`)를 현재 frozen source identity와 validator로 다시 검사해 exact reusable 40, changed/pending 5, invalid 0을 확인했다. 이 결과를 `data/bootstrap-summary-seed.json`에 추적하고 workflow가 verified recovery version `0`일 때만 frozen candidate에 복사하도록 했다. generator의 기존 exact source/provenance/schema/summary validation은 그대로이며 불일치 5개만 Claude pending으로 남는다. v1에서는 seed step을 실행하지 않고, Pages publish도 seed 파일을 참조하지 않는다. focused tests 57/57과 v0 조건을 v1로 뒤집은 mutation RED를 통과했다. 전체 기준선은 Node 547개(535 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass, actionlint 1.7.12 pass(custom self-hosted label 한 항목만 exact ignore), production dependency audit 0이다. 외부 limit 해제 뒤 재개 기준은 seeded replay 또는 controlled run이 5개 pending을 완주하는 것이다.

failure artifact가 보존한 3-byte invariant SHA-256을 frozen README substring 전수 해시와 대조해 exact product가 `iOS`임을 복원했다. English는 `goal`에만 `iOS`를 썼지만 Spanish는 `goal/pros/cons/fit`에 동일 제품명을 자연스럽게 반복했다. 기존 validator는 제품명까지 command/version/number/URL과 같은 exact field distribution으로 강제해 사실 왜곡이 없는 번역을 거부했다. command/version/number/URL은 기존 strict field parity를 유지하고 product는 모든 English-bound field에 반드시 존재하되 translated locale의 추가 자연 언급만 허용한다. missing English-bound product는 계속 RED이며 correction target과 prompt도 extra product 제거를 요구하지 않도록 같은 계약으로 맞췄다. run과 같은 `iOS` fixture는 수정 전 RED, 수정 후 GREEN이고 exact parity를 되돌린 변이는 다시 RED였다. 같은 frozen t3code 입력을 현재 producer로 격리 재생해 logicalCalls 1, attempts 2, retries 1, inputTokens 31454, outputTokens 23617로 성공했다. 최종 local 기준선은 Node 545개(533 pass, 12 intentional skip), Python 146 pass, Firestore Rules 9 pass다.

## 6. 적대적 검증 범위

- collector 성공 전 provider 0회
- incomplete enrichment이면 DB/page/commit/Pages 0
- candidate 실패 시 tracked tree 불변
- missing/0-byte/oversized/malicious README, invalid UTF-8
- GitHub 404/429/500, timeout, retry, pagination 이상
- mutable contents/blob mismatch, head/blob/content hash mismatch, stale cache
- incomplete chunk, schema-invalid model output, locale invariant mismatch, insufficient source
- candidate residue, install collision, duplicate execution, damaged state
- tooltip·README focus trap, BFCache/overlay, keyboard, touch/swipe, light/dark/reduced motion
- CSV/JSON privacy, Atom current/membership split
- secret는 값 대신 존재·길이·digest prefix만 확인

## 7. 현재 browser 증거

- 이 항목은 아직 deploy되지 않은 2026-09-01 local candidate 증거다. current production pass로 세지 않는다.
- 390/720/1200/1440px 모두 horizontal overflow 0. 390/720은 rail `display:none`, 1200/1440은 64px rail `display:flex`다.
- 390/720의 mobile native button은 평소 1x1 visually-hidden이며 `tabIndex=0`, screen-reader access가 열려 있다. keyboard focus 시 71x44 fixed control로 나타났고 Enter 후 focus `sidebarClose`, Escape 후 `mobileNavToggle`로 복귀했다. 1200/1440에서는 `tabIndex=-1`, `inert=true`, `aria-hidden=true`다.
- actual runtime에서 All/daily/weekly/monthly card count는 45/16/20/22다. 기간 rank는 각각 1..N, period gain은 source와 exact 일치하고 export repository array는 rendered card array와 exact 일치한다. All은 gain/HOT/gain bar 0이며 export gain은 전부 null이다.
- desktop hover는 `openMode=hover`, scrim 없음, main inert 아님이다. rail→sidebar 이동은 open을 유지했고 결합 영역 밖으로 이동한 직후 `open=false`; computed close transition은 `0.21s, 0.21s`다.
- English, Korean, Simplified Chinese, Spanish, Japanese 모두 `html lang`, subtitle, search placeholder, Explore label이 실제 전환됐다. tooltip summary control은 0개이며 Japanese exact summary를 표시했다. Japanese `fit`을 runtime에서 비운 mutation은 Japanese unavailable을 표시하고 English goal을 포함하지 않았다.
- reduced-motion emulation에서 sidebar, tooltip, scroll-top transition duration은 모두 0s이고 overflow는 0이다.
- headless CDP는 coarse pointer와 BFCache 복원을 제공하지 않았다. swipe는 actual page runtime에 touch pointer event를 주입해 right-open/left-close를 확인했고, BFCache는 자동 테스트 GREEN만 있으며 actual browser pass로 세지 않는다.
- axe-core 4.12.1은 390/1440에서 violations 0을 반환했지만 passes도 0이라 완전한 accessibility clean 근거로 사용하지 않는다. Impeccable detector도 HTML parser dependency 부재로 degraded regex mode였고 findings 0은 undercount다.
- 사용자의 일반 Chrome에서는 current production 로그인 계정 모드가 reload, 새 탭, `pageshow.persisted=true` BFCache, Chrome 완전 재시작 뒤에도 유지됐고 계정 즐겨찾기 수는 모두 6개였다. App Check warning/error 비식별 집계는 0건이었다.
- current production의 cross-tab logout은 RED다. 로그아웃한 탭은 guest 1개로 즉시 전환됐지만 peer 탭은 10초 뒤에도 account 6개를 유지했고 reload 뒤에만 guest 1개가 됐다. Firebase local persistence 자체는 지워졌지만 peer의 live auth observer 전달이 없었다. `firebase-client.js`에 사용자 식별정보 없는 일회성 same-origin signout 신호를 추가해 peer가 자기 Auth를 로그아웃하도록 보완했으며 focused TDD와 두 변이는 통과했다. 아직 미배포이므로 production pass로 세지 않는다.

## 8. 종료 조건

- 두 번째·세 번째·여섯 번째·일곱 번째·여덟 번째·아홉 번째·열두 번째·열세 번째·열네 번째·열다섯 번째·열일곱 번째·열여덟 번째·열아홉 번째 run에서 실제 Claude enrichment 요청은 시작됐지만 validator 실패로 완성 bundle과 usage receipt가 남지 않아 token count는 `unknown`이다. 스무 번째·스물두 번째·스물네 번째·스물다섯 번째·스물여섯 번째·스물일곱 번째·스물여덟 번째·스물아홉 번째·서른 번째·서른한 번째·서른두 번째는 failure diagnostic에서 input/output token과 attempts/retries를 보존했다. 열 번째·열한 번째·열여섯 번째는 collector에서, 스물한 번째·스물세 번째는 production-state gate에서 종료돼 Claude 호출이 0회였다. production DB·commit·Pages publication은 서른두 run 모두 0회다.
- full tests, adversarial review, mutation, staged/working-tree secret scan이 통과한다.
- push 직전 remote drift를 fetch 후 재판정하고 사용자 변경을 보존한다.
- controlled workflow `33337114759`, `33338119441`, `33340906781`, `33353784160`, `33355529363`, `33356905445`, `33358622337`, `33360314788`, `33361877370`, `33363575020`, `33363947539`, `33365422799`, `33367283210`, `33370143480`, `33372034170`, `33374144042`, `33375183865`, `33378871320`, `33383238462`, `33388405416`, `33392159705`, `33392487673`, `33395938647`, `33397406754`, `33400238848`, `33403148891`, `33407705006`, `33410426347`, `33414846737`, `33429148032`, `33433157231`, `33440722982`은 각각 prepare test, enrichment evidence gate, enrichment invariant gate, runner Worker handoff, reboot 후 runner handoff, Spanish placeholder gate, raw README invariant substring gate, inference field order gate, repeated Spanish cons fallback gate, OSS Insight `freellmapi` 500, OSS Insight `pi` 500, Japanese inference-strength gate, Japanese invariant field gate, English marketing language gate, Spanish invariant field gate, transient GitHub metadata 수집, rendered Markdown version invariant gate, unused English summary invariant gate, `Lakr233/vphone-cli`의 5-defect full-regeneration correction gate, `THU-MAIC/OpenMAIC`의 6-defect token metadata gate, verified-v0 explicit-input gate, `checkstyle/checkstyle`의 source-grounded marketing false-positive gate, Pages CDN transient 503 gate, `K-Dense-AI/scientific-agent-skills`의 Spanish extra invariant/token gate, `majd/ipatool`의 4-locale missing `auth` invariant gate, `handsomestWei/patent-disclosure-skill`의 Japanese token·Korean hedge gate, bounded Claude request diagnostic gate, 같은 frozen input의 K-Dense request gate, `affaan-m/ECC`의 Spanish lowercase `todo` false-positive gate, `pingdotgg/t3code`의 product field-distribution gate, `AprilNEA/OpenLogi`의 Claude subscription rate-limit gate에서 publish 전에 종료됐다. 필요한 후속 dispatch는 승인됐지만 direct API·새 유료 provider는 승인되지 않았다.
- production manifest와 exact file hashes가 deployed source SHA에 묶인다.
- 모든 active repository가 detailed 5-locale summary, valid README provenance, canonical/upstream README variants를 전수 통과한다.
- 하나라도 generic fallback, missing locale, stale source, variant mismatch면 recovery/hold를 유지한다.
- CodeQL analysis는 matching commit과 results count가 있는 실제 analysis로 확인하며 0 analyses를 통과로 세지 않는다.
- 실제 Google 계정 persistence·isolation matrix와 나머지 production acceptance를 완료한다.
- 마지막 clean `HEAD == origin/main == deployed sourceSha`와 schedule 상태를 확인한다.
- 폐기된 Deep Scan은 다시 gate나 잔여 위험으로 올리지 않는다. L1-L5, M1 재설계, 새 확장 후보를 자동 시작하지 않는다.
